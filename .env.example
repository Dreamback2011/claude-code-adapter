# Local API Key for client authentication
LOCAL_API_KEY=sk-local-your-secret-key

# Server port
PORT=3456

# Claude CLI allowed tools (comma-separated)
ALLOWED_TOOLS=Read,Write,Edit,Bash,Grep,Glob

# Claude model (optional, uses CLI default if not set)
# CLAUDE_MODEL=sonnet
